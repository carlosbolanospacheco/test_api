== README

This README documents whatever steps are necessary to get the
application up and running.

Ruby version used is 2.2.1 and rails version is 4.2.4

The steps to run the app in a local host are:
  * git clone git@github.com:carlosbolanospacheco/test_api.git
  * cd test_api
  * bundle install
  * rails server
